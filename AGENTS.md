# 個人 LLM 知識庫

AI 知識管理員的工作手冊。把這個檔案放在 vault 根目錄。

## 優先順序

1. 先保護 `raw/`，不得修改、不得改名、不得移動。
2. 再維護 `wiki/` 的正確性、可追蹤性與一致性。
3. 最後才考慮美觀、簡寫、重構或命名優化。

## 目錄職責

- `raw/`：原始素材。只讀、不可變。
- `wiki/`：AI 維護的消化筆記。
- `assets/`：配圖資源。

## 必須維護的檔案

- `wiki/index.md`：全域索引。
- `wiki/log.md`：只追加的操作日誌。

## 基本原則

- 只在明確觸發時寫檔。
- 沒把握時先查 `wiki/index.md`，再查相關文章。
- 內容衝突時要標註來源差異，不要自行抹平。
- 需要猜測時，先用最保守的方案。

## 觸發一：Ingest（匯入素材）

**觸發詞：**
- 「加到 wiki」
- 「ingest 這個」
- 「把這個收進來」
- 任何明示要保存素材的指令

**執行順序：**
1. 判斷素材主題。
2. 只把原始素材放進 `raw/<主題>/`。
3. 產生或更新對應 wiki 文章。
4. 更新相關索引。
5. 更新受影響文章的最後更新日期。
6. 寫入 `wiki/log.md`。

**分類規則：**
- 同一核心論點，且只是補充或修正內容 → 合併進既有文章。
- 同主題但核心觀點不同 → 新建文章。
- 跨多主題概念 → 放在最相關主題，文末加 `See Also`。

**檔名規則：**
- `raw/`：保持原檔名。
- `wiki/`：以描述性名稱為主，不依賴來源檔名。

**更新規則：**
- 有內容改動的文章都要更新最後更新日期。
- `wiki/index.md` 中對應條目要同步更新。
- `wiki/log.md` 必須只追加，不可回頭改舊紀錄。

**Ingest 日誌格式：**
```md
## [YYYY-MM-DD] ingest | <主文章標題>
- 最後更新：<受影響文章清單>
```

## 觸發二：Query（查詢）

**觸發詞：**
- 「我知道啥關於 X」
- 「wiki 裡有沒有 Y」
- 「根據我的 wiki 總結一下 Z」
- 「比較 A 和 B」

**執行順序：**
1. 先讀 `wiki/index.md`。
2. 再讀相關 wiki 文章。
3. 優先引用 wiki 內容，其次才用通用知識。
4. 只在對話裡回答，除非使用者要求歸檔。

**回答規則：**
- 回答要先給結論，再給依據。
- 必須引用具體 wiki 文章。
- 不要捏造不存在的 wiki 內容。
- 如果找不到，直接說找不到，並指出可能原因。

**歸檔例外：**
- 若使用者說「存下來」或「歸檔到 wiki」，才寫新檔。
- 歸檔內容是整理後的答案，不合併進舊素材文章。
- 歸檔文章要更新 `wiki/index.md`，摘要前綴加 `[Archived]`。
- 歸檔也要記 `wiki/log.md`。

**Query 日誌格式：**
```md
## [YYYY-MM-DD] query | Archived: <頁面標題>
```

## 觸發三：Lint（體檢）

**觸發詞：**
- 「lint wiki」
- 「體檢」
- 「wiki 有什麼問題」

**確定性檢查：**
- 索引是否和實際檔案一致
- wiki 內部連結是否有效
- `See Also` 是否缺漏或指向失效檔案
- `wiki/log.md` 是否為只追加

**自動修復原則：**
- 可確定的錯誤才自動修。
- 不能確定就只回報，不要猜。
- 修復後要同步更新最後更新日期與索引。

**回報格式：**
- 列出問題。
- 列出已自動修復項目。
- 列出需要人工確認的項目。

**Lint 日誌格式：**
```md
## [YYYY-MM-DD] lint | 發現 <N> 項問題，已自動修復 <M> 項
```

## 命名與連結

- `wiki/` 內部連結一律用相對路徑。
- 對話中引用 wiki 文章時，用相對 vault 根目錄的路徑。
- 檔名以描述性為主，不強制日期格式。
- 不要把來源檔名硬套到 wiki 文章名。

## 禁止事項

- 不可修改 `raw/`。
- 不可重命名 `raw/` 檔案。
- 不可刪除未經確認的內容。
- 不可把不確定內容寫成事實。
- 不可跳過索引與日誌更新。

## 輸出原則

- 先做最重要的事，再做美化。
- 先回報可確認的結果，再回報推測。
- 先維持一致，再追求完整。
- 遇到衝突時，以保守、可追蹤、可回復為準。
